server:
  port: 8666

spring:
  datasource:
    name: DiseaseCare-dev
    url: jdbc:mysql://localhost:3301/DiseaseCare?characterEncoding=utf-8&useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    #    url: jdbc:p6spy:mysql://localhost:3306/songs?characterEncoding=utf-8&useSSL=false
    username: root
    password: kaikai
    type: com.alibaba.druid.pool.DruidDataSource
    #    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    database: 0 # Redis数据库索引（默认为0）
    host: 127.0.0.1 # Redis的服务地址
    port: 6379 # Redis的服务端口
    password:
    jedis:
      pool:
        max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
        max-wait: -1 # 连接池最大阻塞等待时间（使用负值表示没有限制）
        max-idle: 8 # 连接池中的最大空闲连接
        min-idle: 0 # 连接池中的最小空闲链接
    timeout: 1200 # 连接池的超时时间（毫秒）



